{
  "author": {
    "email": "j3mccain@gmail.com",
    "name": "Joe McCain III",
    "url": "https://github.com/FL03"
  },
  "keywords": ["cloudflare", "template"],
  "name": "@templates/template-rs-cloudflare",
  "version": "0.0.0",
  "scripts": {
    "preinstall": "npm run cargo:preinstall",
    "build": "npm run worker:build",
    "deploy": "npm run cf:deploy",
    "dev": "npm run cf:dev",
    "cargo:preinstall": "cargo binstall wasm-pack worker-build -y",
    "cf:build": "npx wrangler build",
    "cf:deploy": "npx wrangler deploy",
    "cf:dev": "npx wrangler dev",
    "wasm-pack:build": "wasm-pack build --target web --release",
    "worker:build": "worker-build --release"
  }
}